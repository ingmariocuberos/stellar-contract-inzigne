[package]
name = "inzigne"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = { version = "22.0.0-rc.2", features = ["alloc"] }

[dev-dependencies]
soroban-sdk = { version = "22.0.0-rc.2", features = ["testutils"] }